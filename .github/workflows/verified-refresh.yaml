name: "Debug - Find My Files"

on: 
  workflow_dispatch:
  push:

jobs:
  list_everything:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout json-tools branch
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}
          ref: json-tools
          path: json-tools

      - name: Show Me Everything
        run: |
          echo "--- القائمة الكاملة للملفات في فرع json-tools ---"
          ls -R json-tools
